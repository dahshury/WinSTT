---
title: Models
description: Browse the full WinSTT speech-to-text catalog — 70+ on-device ONNX models across 11 families, with accuracy, speed, size, and language coverage so you can pick the right trade-off.
---

WinSTT ships **70+ speech-to-text models** across 11 families, all running on a single
ONNX engine on your own hardware. Pick one in the [Model tab](/docs/settings/model) and the
app downloads it in the background — nothing about your audio leaves the machine.

<Screenshot
  src="model-dropdown"
  alt="The open model picker showing a maker rail on the left and model rows with accuracy and speed bars, parameter size, language scope, and per-quantization download badges."
  label="Model picker"
  caption="Every model shows its accuracy and speed bars, size, language scope, and which quantizations you can download."
/>

## One engine, many models

Every model — Whisper included — runs through the same inference engine (ONNX Runtime via
the `onnx-asr` library). There is no separate backend to choose: switching models is a
download, not a re-install. Most swaps happen live without restarting the server.

Whether inference runs on the **GPU** or **CPU** is decided by the [installer you
chose](/docs/install) and the **Device** setting, not by the model. Numerical precision is
chosen per-download via [quantization](/docs/models/compute-types).

<StatGrid>
  <Stat value="73" label="models" />
  <Stat value="11" label="families" />
  <Stat value="99" label="languages (Whisper)" />
  <Stat value="5.1" label="best WER (Cohere)" />
</StatGrid>

## The 11 families

Each row sums up one family: how many models it offers, the typical on-disk size at default
precision, the languages it covers, and where it fits best. Lower **WER** (word error rate)
is more accurate; higher **RTFx** (real-time factor) is faster.

<ModelTable
  head={["Family", "Models", "Typical size", "Languages", "Recommended for"]}
  numeric={[1]}
  rows={[
    ["Whisper", "12", "149 MB – 4.5 GB", "99 (multilingual + EN-only)", "The all-rounder — accuracy at every size, broadest language reach"],
    ["Lite-Whisper", "3", "~1.1 GB", "99", "Compressed Whisper Large v3 Turbo — large-model quality at smaller size"],
    ["NeMo", "34", "0.1 – 4 GB", "English, Russian, and 25-lang multilingual", "Fast offline Parakeet / Canary plus native streaming exports"],
    ["Moonshine", "10", "0.1 – 0.7 GB", "EN + per-language fine-tunes", "Lowest-latency real-time on CPU; tiny footprint"],
    ["Cohere", "1", "~5.1 GB", "14", "Maximum accuracy — #1 on the HF Open ASR leaderboard"],
    ["Granite Speech", "2", "2.5 – 16 GB", "en · fr · de · es · pt", "IBM autoregressive and non-autoregressive speech models with strong punctuation"],
    ["Qwen3-ASR", "2", "2.0 – 4.1 GB", "30", "Alibaba Qwen LLM-decoder ASR with auto language detection; int4-only downloads"],
    ["SenseVoice", "1", "159 MB", "5 (zh/en/ja/ko/yue)", "Fast East-Asian multilingual in a tiny int8 graph"],
    ["Dolphin", "1", "104 MB", "15 Asian and Middle Eastern languages", "Compact multilingual CTC model for Eastern-language coverage"],
    ["GigaAM", "2", "~3.5 GB", "Russian", "Best-in-class Russian (CTC / RNNT)"],
    ["Kaldi / Vosk / Zipformer", "4", "93 – 478 MB", "English + Russian", "Lightweight Vosk and Zipformer models for low-end hardware"],
    ["T-One", "1", "144 MB", "Russian", "Compact Russian streaming ASR"],
  ]}
  caption="Sizes are representative downloads; smaller quantizations are usually available. The catalog totals 73 shipped entries plus user-dropped custom models."
/>

## A few standouts

<ModelTable
  head={["Model", "Params", "WER", "RTFx", "Languages", "Default size"]}
  numeric={[1, 2, 3]}
  rows={[
    ["Whisper Tiny", "38M", "24.5", "750", "99", "149 MB"],
    ["Whisper Large v3 Turbo", "796M", "7.8", "250", "99", "1.1 GB"],
    ["Moonshine Base", "62M", "8.5", "990", "EN", "711 MB"],
    ["Parakeet TDT 0.6B v3", "627M", "5.9", "1100", "25", "2.5 GB"],
    ["Canary 1B v2", "978M", "5.6", "430", "25", "4.0 GB"],
    ["Cohere Transcribe", "2.0B", "5.1", "270", "14", "5.1 GB"],
    ["Qwen3-ASR 0.6B", "600M", "—", "—", "30", "2.0 GB"],
    ["Granite Speech 4.1 2B Plus", "2.0B", "—", "—", "5", "4.1 GB int8"],
    ["SenseVoice Small", "234M", "7.2", "850", "5", "159 MB"],
  ]}
  caption="WER is an averaged reference figure across benchmark sets; your numbers vary with audio, language, and quantization. RTFx is throughput relative to real time on GPU."
/>

## How to pick

There is no single best model — it's a trade-off between **accuracy**, **speed**, **size**,
and **language**. Start from what matters most to you.

<BentoGrid cols={2}>
  <BentoCell icon="quality" title="Maximum accuracy">
    **Cohere Transcribe** (WER 5.1) leads, followed by **Canary 1B v2** and **Parakeet TDT
    0.6B v3** for European languages, or **Whisper Large v3** for the widest language reach.
  </BentoCell>
  <BentoCell icon="speed" title="Lowest latency">
    **Moonshine** (up to 1800 RTFx) is built for real-time English on CPU. For accurate
    English fast, **Parakeet** clears 1000 RTFx.
  </BentoCell>
  <BentoCell icon="cpu" title="Smallest footprint">
    **Whisper Tiny** (149 MB), **SenseVoice Small** (159 MB), or **Moonshine Tiny** ship the
    smallest defaults; q4 / int8 quantizations shrink them further.
  </BentoCell>
  <BentoCell icon="languages" title="Specific language">
    **Whisper** covers 99 languages; **Qwen3-ASR** 30; **Cohere** 14; **Canary /
    Parakeet v3** 25 European languages; **GigaAM / Vosk / T-One** are Russian;
    **Moonshine** has per-language fine-tunes.
  </BentoCell>
</BentoGrid>

<Callout type="info" title="Good default">
  If you're unsure, start with **Whisper Large v3 Turbo** (multilingual, WER 7.8, ~1.1 GB) or
  **Parakeet TDT 0.6B v3** for fast, accurate English and European dictation. Small machines
  can begin with **Whisper Tiny** or **Moonshine Base** and grow from there.
</Callout>

<Callout type="warn" title="Real-time preview needs a small model">
  The [live preview](/docs/settings/quality) only runs models under ~700M parameters. Pair a
  large main model with a small **realtime model** (Whisper Tiny, Moonshine, or Canary 180M
  Flash) so words still stream as you speak.
</Callout>

## Quantizations and downloads

Most models offer several **quantizations** — compressed weight formats (`fp16`, `int8`,
`q4`, `q4f16`, `bnb4`) that trade a little accuracy for big wins in size and speed. The
picker only shows the variants the upstream repo actually publishes, and you can download,
pause, resume, or delete each one independently. See
[Compute & quantization](/docs/models/compute-types) for the full breakdown.

<Callout type="success" title="Everything stays local">
  Models download once into your user data folder and run entirely on-device. Audio is
  processed in memory and never uploaded — switching models never changes that. The only
  exception is the optional [cloud STT providers](/docs/settings/integrations), which you opt
  into explicitly with an API key.
</Callout>

## Related

<Cards>
  <Card title="Whisper & Lite-Whisper" href="/docs/models/whisper">
    OpenAI Whisper, the EN-only variants, and the compressed Lite-Whisper builds.
  </Card>
  <Card title="NeMo Parakeet & Canary" href="/docs/models/nemo">
    NVIDIA's high-accuracy Conformer models for English, multilingual, and Russian.
  </Card>
  <Card title="Other models" href="/docs/models/other">
    Moonshine, Cohere, Granite, Qwen3-ASR, SenseVoice, Dolphin, GigaAM, Vosk/Kaldi, and T-One.
  </Card>
  <Card title="Compute & quantization" href="/docs/models/compute-types">
    What fp16, int8, q4, and q4f16 mean — and how to read the picker's badges.
  </Card>
</Cards>
