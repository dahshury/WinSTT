#!/usr/bin/env sh
set -eu

[ "${HUSKY:-1}" = "0" ] && exit 0
[ "${WINSTT_SKIP_GIT_HOOKS:-0}" = "1" ] && exit 0

echo "winstt pre-push: checking lockfile, tests, dead code, and Rust fmt/clippy"
bun run prepush
