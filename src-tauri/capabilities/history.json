{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "history-capability",
  "description": "History window clipboard fallback for copying transcript text.",
  "windows": ["history"],
  "permissions": ["clipboard-manager:allow-write-text"]
}
