# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/
.agents/
.codex/
skills-lock.json
.deep-research/

/target/
/recording_*
.crush/

# Playwright
playwright-report/
test-results/
blob-report/
output/

.direnv
.envrc

# Nix build output
result

# WinSTT reference clone (not part of the fork)
examples/WinSTT/
.i18n-work/

# Reference example clones (embedded repos, not part of the fork) — keep only
# the reference app, which IS part of this repo.
examples/*
!examples/winstt-electron/

# Custom cargo target dirs (spike / moon / verify builds). `/target/` is already
# ignored above; these are alternate build outputs that must never be committed.
src-tauri/target*/
target-moon/
target-spike/
target-verify*/

# Python tooling cache (ruff) — stray ephemeral cache, never commit
.ruff_cache/

# TTS engine-spike model cache (kitten/piper/supertonic/chatterbox/oute weights,
# loaded by src-tauri/examples/tts_engine_spike.rs). Re-downloadable; never commit.
.tts-cache/

tools/windows/*.log
spike_out.log
