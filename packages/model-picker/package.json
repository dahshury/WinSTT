{
  "name": "@winstt/model-picker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "description": "Self-contained OpenRouter / Ollama model-picker widget. No external WinSTT @/shared/* couplings; consumer supplies labels via a t(key) function and the OpenRouter / Ollama model shapes via props. Vendor copies of the small UI primitives it needs (Badge, Tooltip wrappers, cn) live under src/vendor/.",
  "peerDependencies": {
    "react": ">=19",
    "@base-ui/react": ">=1.4",
    "@hugeicons/react": ">=1",
    "@hugeicons/core-free-icons": ">=4",
    "virtua": ">=0",
    "fuse.js": ">=7"
  }
}
